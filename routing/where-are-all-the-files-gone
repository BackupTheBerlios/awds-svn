
The AWDS development has switched from using SVN for managing the source
to an better alternative - git. We have deleted all files to avoid confusion.

If required, you can grab the old sources by digging in the SVN history. 

For a list of git repositories, have a look in the corresponding file.

